import { Box, Typography } from './Header.styled';

const Header = () => {
    return (
        <Box className="header">
            <Typography>Header Area</Typography>
        </Box>
    );
};

export default Header;
